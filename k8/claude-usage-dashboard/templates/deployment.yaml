{{- if and (eq .Values.claudeData.mode "existingPvc") (not .Values.claudeData.existingClaim) }}
{{- fail "claudeData.existingClaim is required when claudeData.mode is existingPvc" }}
{{- end }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "cud.fullname" . }}
  labels:
    {{- include "cud.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "cud.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "cud.selectorLabels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if or .Values.serviceAccount.create .Values.serviceAccount.name }}
      serviceAccountName: {{ include "cud.serviceAccountName" . }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: app
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: {{- toYaml .Values.command | nindent 12 }}
          {{- with .Values.args }}
          args: {{- toYaml . | nindent 12 }}
          {{- end }}
          ports:
            - name: http-dashboard
              containerPort: 3333
              protocol: TCP
            - name: http-proxy
              containerPort: 8080
              protocol: TCP
          env:
            - name: HOME
              value: /root
            - name: ANTHROPIC_PROXY_BIND
              value: "0.0.0.0"
            {{- with .Values.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
            {{- if .Values.sync.tokenFromSecret }}
            - name: CLAUDE_USAGE_SYNC_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.sync.tokenFromSecret.name }}
                  key: {{ .Values.sync.tokenFromSecret.key }}
            {{- else if include "cud.appSecretName" . | trim }}
            {{- $sn := include "cud.appSecretName" . | trim }}
            - name: CLAUDE_USAGE_SYNC_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ $sn }}
                  key: {{ .Values.secrets.keys.syncToken }}
                  optional: true
            {{- end }}
            {{- if include "cud.appSecretName" . | trim }}
            {{- $sn := include "cud.appSecretName" . | trim }}
            - name: GITHUB_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ $sn }}
                  key: {{ .Values.secrets.keys.githubToken }}
                  optional: true
            - name: CLAUDE_USAGE_ADMIN_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ $sn }}
                  key: {{ .Values.secrets.keys.adminToken }}
                  optional: true
            {{- end }}
            {{- if .Values.config.scanIntervalSec }}
            - name: CLAUDE_USAGE_SCAN_INTERVAL_SEC
              value: {{ .Values.config.scanIntervalSec | quote }}
            {{- end }}
            {{- if .Values.config.syncMaxMb }}
            - name: CLAUDE_USAGE_SYNC_MAX_MB
              value: {{ .Values.config.syncMaxMb | quote }}
            {{- end }}
            {{- if .Values.config.logLevel }}
            - name: CLAUDE_USAGE_LOG_LEVEL
              value: {{ .Values.config.logLevel | quote }}
            {{- end }}
            {{- if .Values.config.githubReleasesFetch }}
            - name: CLAUDE_USAGE_GITHUB_RELEASES_FETCH
              value: {{ .Values.config.githubReleasesFetch | quote }}
            {{- end }}
            {{- if .Values.extraBases }}
            - name: CLAUDE_USAGE_EXTRA_BASES
              value: {{- range $i, $b := .Values.extraBases }}{{ if $i }};{{ end }}{{ $b.mountPath }}{{- end }}
            {{- end }}
          {{- with .Values.livenessProbe }}
          livenessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.readinessProbe }}
          readinessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- if .Values.resources }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          {{- end }}
          volumeMounts:
            - name: claude-data
              mountPath: /root/.claude
            {{- range .Values.extraBases }}
            - name: {{ .name }}
              mountPath: {{ .mountPath }}
              readOnly: {{ .readOnly | default false }}
            {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      volumes:
        - name: claude-data
          {{- if eq .Values.claudeData.mode "emptyDir" }}
          emptyDir: {}
          {{- else if eq .Values.claudeData.mode "hostPath" }}
          hostPath:
            path: {{ .Values.claudeData.hostPath.path }}
            type: {{ .Values.claudeData.hostPath.type }}
          {{- else }}
          persistentVolumeClaim:
            claimName: {{ include "cud.claudePvcName" . }}
          {{- end }}
        {{- range .Values.extraBases }}
        - name: {{ .name }}
          {{- if .existingClaim }}
          persistentVolumeClaim:
            claimName: {{ .existingClaim }}
          {{- else if .hostPath }}
          hostPath:
            path: {{ .hostPath }}
            type: Directory
          {{- end }}
        {{- end }}
