Deployed {{ include "cud.fullname" . }} in namespace **{{ .Release.Namespace }}** (Ziel: **claude**).

Reach the UI:
{{- if .Values.ingress.enabled }}
  https://{{ (index .Values.ingress.hosts 0).host }}/ (per Ingress TLS / host rules)
{{- else if eq .Values.service.type "NodePort" }}
  NodePort dashboard: {{ .Values.service.nodePortDashboard }}  proxy: {{ .Values.service.nodePortProxy }}
{{- else }}
  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "cud.fullname" . }} 3333:{{ .Values.service.portDashboard }} 8080:{{ .Values.service.portProxy }}
{{- end }}

Secrets (optional keys; fehlende Keys sind mit optional:true unkritisch):
{{- if .Values.secrets.create }}
  Helm hat Secret {{ include "cud.appSecretName" . }} aus values.secrets.stringData erzeugt.
{{- else if .Values.secrets.existingSecret }}
  App-Secret-Name: {{ .Values.secrets.existingSecret }} (Keys: {{ .Values.secrets.keys.syncToken }}, {{ .Values.secrets.keys.githubToken }}, {{ .Values.secrets.keys.adminToken }})
{{- else if .Values.sync.tokenFromSecret }}
  Nur Sync-Token aus Secret {{ .Values.sync.tokenFromSecret.name }} (Key {{ .Values.sync.tokenFromSecret.key }}).
{{- else }}
  Kein App-Secret konfiguriert — Sync/GitHub/Admin per values.secrets oder sync.tokenFromSecret setzen.
{{- end }}

Push JSONL from your machine into the pod volume:
  export CLAUDE_SYNC_URL=https://<dashboard-url>
  export CLAUDE_SYNC_TOKEN=<sync-token>
  node scripts/claude-data-sync-client.js

Details: k8/README.md · Beispiel-Values: values-cluster.example.yaml
