# Wie GRO/SCHUFA k8s/overlays/dev/kustomization.yml (feat/fastify-v5).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: claude

resources:
  - ../../base
  - namespace.yml

# CI pushed tags: latest + commit SHA — Kustomize-Tag für ersten Apply (set image setzt SHA nach).
images:
  - name: harbor.grosswig-it.de/claude/claude-usage-dashboard
    newTag: latest
